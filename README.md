# RecipeApp
Full stack webapp built using MERN stack. Uses spoonacular API to recipe data and user can add recipes to a favorite list.
To run on local-
First install all the packages by - npm install
To start both backend and frontend , go to root directories of both in the Terminal and run "npm run dev" for backend and  "npm start" for frontend
